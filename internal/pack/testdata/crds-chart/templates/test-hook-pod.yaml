apiVersion: v1
kind: Pod
metadata:
  name: crds-chart-test
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: probe
      image: busybox
      command: ["true"]
