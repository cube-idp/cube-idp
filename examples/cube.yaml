apiVersion: cube-idp.dev/v1alpha1
kind: Config
metadata:
  name: dev
spec:
  cluster:
    provider: kind
    # Add forProvider (kind.x-k8s.io/v1alpha4 Cluster fields) to take
    # over the generated config wholesale — including the ingress-ready
    # label and 8080/8443 host-port mappings the default provides.
